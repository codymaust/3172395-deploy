---
name: workflow deploy

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - ready_for_review
      - reopened
      - synchronize
    branches:
      - main

jobs:
  test:
    name: test for 3172395 - workflow deploy
    environment: dev
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: run
        run: |
          echo "hello from workflow deploy"

  test_reusable:
    name: test for 3172395 with reusable workflow
    uses: codymaust/3172395-reusable-workflows/.github/workflows/reusable-1.yml@main
    with:
      environment: dev
      # ref: ${{ github.ref }} # TODO: testing to see if this is related
